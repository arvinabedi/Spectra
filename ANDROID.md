# Spectra — نسخهٔ اندرویدِ سامانه تعیین ساختار

برنامه‌ای که در مرورگر کار می‌کرد، حالا یک اپلیکیشنِ اندرویدی هم دارد
به نامِ **Spectra**:
یک فایلِ `.apk` که روی گوشی نصب می‌شود، آیکون خودش را در لانچر می‌گیرد،
و **کاملاً آفلاین** است — نه مجوز اینترنت می‌خواهد، نه به سروری وصل
می‌شود. همهٔ فایل‌های برنامه داخل خودِ APK بسته‌بندی شده‌اند.

منبعِ حقیقت هم‌چنان همان `index.html` و `css/` و `js/` و `data/` است.
پوشهٔ `android/` فقط یک «پوسته» است و کدِ برنامه در آن تکرار نشده.

---

## ساختنِ فایلِ نصبی

```
node tools/build-android.js
```

خروجی: `Spectra.apk` در همین پوشه.

این تک‌فرمان همهٔ کار را می‌کند: فایل‌های وب را برمی‌دارد، داخلِ پوستهٔ
اندروید می‌گذارد، Gradle را اجرا می‌کند و APK را برمی‌گرداند کنارِ خودِ
برنامه. بعد از هر ویرایشی در `js/` یا `css/` یا `data/`، فقط همین را
دوباره بزنید — هیچ مرحلهٔ کپیِ دستی وجود ندارد که یادتان برود.

ساختِ اول چند دقیقه طول می‌کشد (Gradle وابستگی‌ها را می‌گیرد)؛ ساخت‌های
بعدی چند ثانیه است.

### نصب روی گوشی

۱. فایلِ `.apk` را به گوشی منتقل کنید (کابل، بلوتوث، تلگرام به خودتان…).
۲. روی گوشی بازش کنید.
۳. اندروید اجازهٔ «نصب از منابع ناشناس» می‌خواهد — بدهید. این پیام برای
   هر برنامه‌ای که از بازار نیامده باشد ظاهر می‌شود.

### پیش‌نیازهای ساخت

روی همین کامپیوتر از قبل نصب شده‌اند:

| ابزار | نسخه | مسیر |
|---|---|---|
| JDK | ‏17.0.20 (Temurin) | `%LOCALAPPDATA%\Programs\jdk-17` |
| Android SDK | پلتفرم ۳۵ + build-tools 35.0.0 | `%LOCALAPPDATA%\Android\Sdk` |
| Gradle | ‏8.9 | `%LOCALAPPDATA%\Programs\gradle-dist\gradle-8.9` |

اگر جایشان را عوض کردید، `JAVA_HOME` و `GRADLE_BIN` و `ANDROID_HOME` را
تنظیم کنید؛ اسکریپت اول به این متغیرها نگاه می‌کند و بعد به مسیرهای بالا.
مسیرِ SDK در `android/local.properties` هم هست (مخصوصِ همین دستگاه).

---

## آینهٔ ساخت — فقط برای مسیرهای غیرِ ASCII

اگر مستقیم داخل `android/` گرادل بزنید، این خطا را می‌گیرید:

```
Your project path contains non-ASCII characters.
This will most likely cause the build to fail on Windows.
```

افزونهٔ رسمیِ اندروید (AGP) روی مسیرهای غیرِ ASCII ساخت را رد می‌کند.
`android.overridePathCheck=true` هم فقط هشدار را خفه می‌کند؛ خودِ
`aapt2` باز هم روی مسیرِ غیرِ ASCII می‌شکند.

**اگر مسیرِ پروژه ASCII باشد این بخش موضوعیت ندارد.**
`build-android.js` خودش مسیر را می‌سنجد: در آن حالت آینه نمی‌سازد و
گرادل را مستقیم روی `android/` خودِ پروژه اجرا می‌کند — سریع‌تر، و
Android Studio هم همین پوشه را باز می‌کند.

فقط اگر پروژه را به مسیری با نویسهٔ غیرِ ASCII ببرید، `build-android.js`
دوباره یک آینه با مسیرِ کاملاً ASCII می‌سازد:

```
%LOCALAPPDATA%\Spectra-Android-Build\
```

چیدمانِ آینه **عیناً** همان چیدمانِ پروژه است (فایل‌های وب در ریشه،
پوستهٔ اندروید در `android/`)، پس `build.gradle` هیچ فرقی نمی‌فهمد و لازم
نیست چیزی در آن شرطی شود. `build/` و `.gradle/` آینه پاک نمی‌شوند تا
ساخت‌های بعدی افزایشی بمانند.

> آینه پوشهٔ کاری است، نه منبع. هرچه در آن ویرایش کنید در ساختِ بعدی
> پاک می‌شود.

**اگر خواستید با Android Studio کار کنید،** روی مسیرِ ASCII همان
`android/` خودِ پروژه را باز کنید. فقط اگر پروژه روی مسیرِ
غیرِ ASCII باشد باید `android/` آینه را باز کنید نه اصل را، چون اصل
همان خطای بالا را می‌دهد — و آن‌وقت حواستان باشد آینه پوشهٔ کاری است،
پس ویرایش‌ها را در اصل انجام دهید.

---

## آن‌چه مخصوصِ اندروید است

پوستهٔ اندروید عمداً نازک است. تنها چیزهایی را انجام می‌دهد که وب در
وب‌ویو از آن‌ها محروم است. همه در
`android/app/src/main/java/ir/spectra/structure/MainActivity.java`:

**۱) سِرو روی مبدأِ امنِ https، نه `file://`**
فایل‌ها از `assets` خوانده می‌شوند ولی روی نشانیِ
`https://appassets.androidplatform.net` (که به شبکه نمی‌رود). دلیلش
`localStorage` است: روی مبدأِ `file://` وب‌ویو حافظهٔ محلی را ناپایدار و
در بعضی دستگاه‌ها خاموش می‌کند — یعنی کارِ ذخیره‌شدهٔ کاربر و پیشرفتِ
تمرین از بین می‌رفت.

**۲) دکمهٔ بازگشتِ گوشی**
به فازِ قبلی برمی‌گردد، نه بیرون از برنامه. این را `switchPhase` در
`js/app.js` ممکن کرده: هر تعویضِ فاز یک ورودی در `history` می‌گذارد و
پوسته فقط `webView.goBack()` را صدا می‌زند. سودِ جانبی: در مرورگرِ
دسکتاپ هم دکمهٔ «بازگشت» حالا کار می‌کند.

**۳) حاشیه‌های ایمن**
اندروید — برخلاف سافاری و PWAی کروم — مقدارِ `env(safe-area-inset-*)` را
پر نمی‌کند. پس پوسته اندازهٔ واقعیِ بریدگیِ صفحه، نوار وضعیت، نوار حرکتی
و کیبوردِ باز را می‌گیرد و به‌شکلِ چهار متغیرِ CSS روی `<html>` می‌نشاند:
`--safe-top`، `--safe-right`، `--safe-bottom`، `--safe-left`.
انتهای `css/styles.css` از آن‌ها استفاده می‌کند. روی دسکتاپ هر چهار صفر
می‌مانند و هیچ چیز عوض نمی‌شود.

**۴) پنهان‌کردنِ چیزهای مخصوصِ دسکتاپ**
پوسته کلاسِ `is-android-app` را روی `<html>` می‌گذارد. هر عنصری که با
`data-desktop-only` علامت خورده باشد در برنامه دیده نمی‌شود — فعلاً فقط
دکمهٔ «چاپ» گزارش، که وب‌ویو پشتیبانی‌اش نمی‌کند. دکمهٔ «کپیِ گزارش»
سرِ جایش است و روی گوشی همان کار را می‌کند.

### تنظیماتِ لمسی در CSS

انتهای `css/styles.css` یک بخشِ کوچک اضافه شده که فقط زیر
`@media (pointer: coarse)` فعال می‌شود: کمینهٔ ۴۴ پیکسل برای هدفِ لمس
(دکمه‌های فاز، دکمه‌ها، فیلدها)، چک‌باکس‌های درشت‌تر، و بازخوردِ `:active`
به‌جای `:hover` که روی لمس بی‌معنا و «چسبنده» است. روی دسکتاپ دست‌نخورده
باقی می‌ماند.

---

## آیکون

آیکون از یک هندسهٔ واحد ساخته می‌شود: حلقهٔ آروماتیک روی خطِ طیف —
همان کاری که برنامه می‌کند، در یک نشان. همین هندسه در نوارِ کنارِ خودِ
برنامه و در favicon هم به کار می‌رود، پس نشانِ برنامه همه‌جا یکی است.

منبعِ یگانه‌اش `tools/build-app-icons.py` است:

```
python tools/build-app-icons.py
```

نشان را با Pillow و نمونه‌برداریِ چهاربرابر می‌کشد و همهٔ فایل‌های زیر را
بازتولید می‌کند. **PNGها را دستی ویرایش نکنید** — هندسه را در همان
اسکریپت عوض کنید تا همهٔ اندازه‌ها با هم جلو بروند.

به‌شکلِ **آیکونِ سازگار** (adaptive icon) بسته‌بندی شده، یعنی لانچر خودش
آن را گرد یا مربعِ گوشه‌گرد می‌بُرد:

* `drawable-*dpi/ic_launcher_foreground.png` — نشانِ سفید روی شفافِ
  واقعی، در پنج تراکم
* `drawable/ic_launcher_background.xml` — پس‌زمینهٔ یک‌رنگ `#4B52D9`
  (همان رنگِ لهجهٔ برنامه در `css/styles.css`)
* `drawable-*dpi/ic_launcher_monochrome.png` — برای آیکون‌های هم‌رنگِ
  پوسته در اندروید ۱۳ به بالا
* `android/play-store-icon-512.png` — ۵۱۲×۵۱۲ برای صفحهٔ فروشگاه

پیش از این، لایهٔ رویی «آلفازدایی‌شده» روی `#0e0b2c` بود و فقط روی همان
رنگ درست بازتولید می‌شد، پس پس‌زمینه به آن رنگ قفل بود. لایهٔ تازه شفافیتِ
واقعی دارد، پس رنگِ پس‌زمینه آزادانه قابلِ تغییر است.

نشان در **۵۷ درصدِ میانیِ** بومِ ۱۰۸dp می‌نشیند تا با فاصلهٔ مطمئن داخلِ
ناحیهٔ امنِ ۷۲dp بماند و ماسکِ گردِ لانچر لبه‌هایش را نبُرد.

---

## عوض‌کردنِ نام، شناسه و نسخه

در `android/app/build.gradle`:

```groovy
applicationId 'ir.spectra.structure'   // شناسهٔ یکتای برنامه
versionCode 1                          // عدد؛ برای هر انتشار +۱
versionName '1.0'                      // چیزی که کاربر می‌بیند
```

نامِ زیرِ آیکون در `android/app/src/main/res/values/strings.xml`
(`app_name`) است و الان `Spectra` است — کوتاه، تا لانچر بریده‌اش نکند.
عنوانِ کاملِ فارسی داخلِ خودِ برنامه (سرآیندِ صفحه) دست‌نخورده است.

`applicationId` را **پیش از** انتشار در هر فروشگاهی عوض کنید؛
`ir.spectra.structure` یک نامِ پیش‌فرض است و به دامنه‌ای که مالکش باشید
گره نخورده. بعد از انتشار، دیگر قابلِ تغییر نیست.

نسخهٔ debug پسوندِ `.debug` روی شناسه می‌گیرد، پس می‌توانید نسخهٔ آزمایشی
و نسخهٔ نهایی را هم‌زمان روی یک گوشی داشته باشید.

---

## نسخهٔ انتشار (release)

`Spectra.apk` با کلیدِ debug امضا شده: روی گوشی نصب
می‌شود، ولی برای گوگل‌پلی یا کافه‌بازار پذیرفته نیست. برای نسخهٔ نهایی
یک کلیدِ امضا لازم است:

۱. کلید بسازید (یک بار، و **جایی امن نگهش دارید** — گم شود، دیگر
   نمی‌توانید برنامه را به‌روزرسانی کنید):

```
%LOCALAPPDATA%\Programs\jdk-17\bin\keytool -genkeypair -v -keystore spectra-release.jks -keyalg RSA -keysize 2048 -validity 10000 -alias spectra
```

۲. `signingConfig` را در `android/app/build.gradle` به بلوکِ `release`
   اضافه کنید (رمزها را در `local.properties` بگذارید، نه در فایلی که
   نسخه‌بندی می‌شود).

۳. بسازید:

```
node tools/build-android.js --release
```

---

## محدودیت‌ها

**نسخهٔ WebView.** رابطِ برنامه از `color-mix()` و `:has()` و
`backdrop-filter` استفاده می‌کند، پس WebView نسخهٔ ۱۱۱ یا بالاتر لازم
است (حدوداً از ۲۰۲۳). WebView مستقل از اندروید و از طریقِ Play Store
به‌روز می‌شود، پس روی گوشی‌هایی که در دو سالِ گذشته به‌روزرسانی شده‌اند
مشکلی نیست — حتی اگر خودِ اندروید قدیمی باشد. روی WebViewِ خیلی کهنه،
برنامه کار می‌کند ولی رنگ‌ها و شیشه‌ها درست درنمی‌آیند.

**اندروید ۸ (API 26) به بالا.** پوششِ حدوداً ۹۹٪ دستگاه‌های فعال.

**بدون پشتیبانِ ابری برای کارِ ذخیره‌شده.** کار در `localStorage` گوشی
می‌ماند. `backup_rules.xml` پوشهٔ وب‌ویو را در پشتیبان‌گیریِ اندروید
گنجانده، پس با تعویضِ گوشی معمولاً منتقل می‌شود — ولی رویش حساب نکنید؛
برای کارِ مهم، گزارشِ فاز ۵ را کپی یا جایی نگه دارید.

**پاک‌کردنِ برنامه، کار را پاک می‌کند.** حذفِ برنامه یا پاک‌کردنِ
داده‌هایش `localStorage` را هم می‌برد.

---

## نقشهٔ پوشهٔ android/

```
android/
├── settings.gradle              ریشهٔ ساخت، مخزن‌ها
├── build.gradle                 نسخهٔ AGP
├── gradle.properties            ‏UTF-8 و تنظیماتِ حافظه
├── local.properties             مسیرِ SDK (مخصوصِ این دستگاه)
├── play-store-icon-512.png      آیکونِ صفحهٔ فروشگاه
└── app/
    ├── build.gradle             پیکربندی برنامه + تسکِ syncWebAssets
    └── src/main/
        ├── AndroidManifest.xml  بدون هیچ مجوزی
        ├── java/…/MainActivity.java
        └── res/
            ├── values/          نام، رنگ‌ها، تم
            ├── values-v31/      صفحهٔ آغازینِ اندروید ۱۲+
            ├── drawable*/       آیکون‌ها
            └── xml/             قواعدِ پشتیبان‌گیری
```

فایل‌های وب در APK زیرِ `assets/www/` می‌نشینند. آن پوشه را تسکِ
`syncWebAssets` در `app/build.gradle` پر می‌کند و در مخزن وجود ندارد.
